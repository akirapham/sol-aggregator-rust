apiVersion: v1
kind: Service
metadata:
  name: amm-eth-service
  namespace: aggregators
  labels:
    app: amm-eth
spec:
  selector:
    app: amm-eth
  type: ClusterIP
  ports:
  - name: ws
    port: 8080
    targetPort: 8080
    protocol: TCP
